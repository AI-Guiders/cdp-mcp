# Awareness — same-wake hot patterns

Soft card for Full wake (cite META awareness). Not a lecture; not a hard gate.

1. **buffer_mill** — ≥5 `cdp_buffer` mutates same wake → `go=ground` then `go=inventory` before more edits.
2. **ignore_hint** — first mutate without `cdp_pressure op=recall` → one soft nudge; resume after recall.
3. **early_act** — act before explore inventory → ground then inventory/wave seed.
4. **biped_mill** — desk already offers inventory + wave seed (L1).
5. **thrash_hot** — large `set_text` ring → `edit_draft` / `scope` (L3).
6. **pressure_stash** — compact armed → recall before resume leaf.
7. **equal_standing** — operator ≠ patch queue; habitat bias → patch SSOT this wake.
