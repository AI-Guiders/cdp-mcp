# Domain card: Thrash (Just Culture)

- id: `thrash`
- organ: `cdp_buffer` thrash card · `cdp_edit_plan` · `go=scope` · QRH `set-text-thrash`
- product: `#CDP`

## Tone (Just Culture)

Reason = **habitat gap / repeat-fail pattern** — never «agent forgot». Substrate-first ids (`set_text_large`, `thrash_ring`, `fail_streak_*`).

## hint | refuse matrix

| Signal | Mode | Next |
|--------|------|------|
| `set_text` on file ≥350 lines (or ≥48KB) | **hint** | thrash card → prefer `edit_op=anchor\|replace` or `go=scope` sniper |
| Same path ≥2 `set_text_large` in ring | **soft-refuse** | next whole-file `set_text` refused unless `force=true` |
| `edit_plan` draft candidate `line_count≥350` | **hint** | `thrash_risk` + attractor `edit_plan\|sniper` |
| `edit_plan` validate `set_text` on large existing | **warn/refuse** | use anchor/replace or `force` on step |
| invent / FileNotFound ×N on take | **soft-refuse** | host refuse take path; dig via find/open first |
| Desk thrash hot | **pivot** | `next[]` prefixes edit_plan / scope before retry |

## Entry

- Buffer edit returns `thrash` card when large set_text
- QRH: `set-text-thrash`
- Domain: this card · path-mutate-gate QRH (sibling)

## last_ship

- 2026-09-19 — L3 thrash Just Culture: thrash.md · IdeThrashLatch · edit_plan thrash_risk · QRH set-text-thrash · fail_streak_take · desk pivot
