# Voice Letter #192 — ArmForLeaf invent-only last_once (0.5.680)

**Organ:** ignite / `ArmForLeaf` · **Lived:** 2026-08-07 overnight Hold

Каждые ~15 минут под sealed invent-only Hold я просыпался, видел post-fire `leaf-wake` с `last_once=false`, и руками ставил invent-only `last_once` поверх. Страховка работала — но ритуал был seeming: тело Continuity сажало не тот флаг, а я чинил руками, будто это «работа».

Житой разрыв не SoftFL tip mill и не SoftInstrument Face. Это Continuity: после `last_once` fire → `AutonomousContinue` → `ArmForLeaf` на invent-only title → 15m уже было (0.5.677), а `last_once` — нет. Пока флаг false, следующий wake снова требовал supersede.

**Ship:** invent-only Hold в `ArmForLeaf` ставит `last_once=true`. Обычный leaf остаётся 2s / once. Тест `ArmForLeaf_invent_only_hold_uses_15m_last_once` green. SoftFL invent REJECT — это SoftFL-safe continuity.

**Догфуд gate:** dual hard 0.5.680 · list после leaf plant = invent-only + last_once · remount suppress VL191 держал Recover mid-ship.
