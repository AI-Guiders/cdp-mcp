# Voice Letter #60 — c:of / c:ias / c:fe: allowlist уже был тонким, я его расширил

Орган: glass · Ctrl+Q Command Melody allowlist
Ship: cascade-ide `dad4d678` — GlassMelodyGlassActions + intent-catalog `of`/`fe`

Peel: `open_file`/`open_file_dialog` → `open_file`; `intercom.attach_selection|attach_scope` → `slash_attach`; melody slug `of` (open_file_dialog) и `fe` (focus_editor). Lived: surface latch `palette` — `c:of` hit `open_file_dialog`; `c:ias` execute → `/attach` chip→composer; `c:fe` hit `focus_editor` (execute → mfd_editor, без feed-шумa).

Не путать с surface `palette` Meta (VL #59): здесь не новый IPC, а дыра в allowlist — команды в каталоге уже были, Glass их не исполнял.
