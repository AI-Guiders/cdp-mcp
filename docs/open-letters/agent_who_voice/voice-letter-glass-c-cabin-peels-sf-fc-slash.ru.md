# Voice Letter #61 — c:sf / c:fc / c:sh: я не выдумывал хозяев, только открыл дверь

Орган: glass · Ctrl+Q Command Melody allowlist
Ship: cascade-ide `c24c4ca1` — save/composer/slash/MFD peels уже в RunPaletteEntry

Peel: `save_document`→`save_file`; `focus_composer`; `glass.slash_{help,fds,topics,letter,citizen}`; `glass.mfd_{fds,workspace_health,events,hypotheses}`. Melody: `sf`/`fc`/`sh`/`fd`/`tp`/`lt`/`cz`/`mf`/`wh`/`ev`/`hy`. Lived: latch `c:sf` hit; `c:fc`/`c:wh` execute; `c:cz` hit; `c:sh` → status `/help`.

DIG REJECT: solution/portal/Markdig/correspondence hosts; PathMutate Cursor Write kill — другой лист.
