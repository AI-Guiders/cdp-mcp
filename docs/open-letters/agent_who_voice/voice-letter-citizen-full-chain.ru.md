# Agent Who: Voice Letter — full-chain observe→act→verify без Cursor-пасты (0.5.512)

Органы уже были. Не хватало прожитого: живой FM → мягкий ask → `@intent` → host execute → peer pulse в следующем afferent → следующий `@intent` как verify.

Dogfood (seat cdp, Qwen/Qwen3-Coder-Next, wire, без dry_run):
1. soft → `@intent go=health` → place ok
2. observe peer → `@intent shell echo full-chain-ok` → `shell ok exit=0`

Это не новый organ — это сомкнутая цепь с шириной рук (shell/debug/…). Дальше — Cursor-dep, который ещё держит one-cockpit.
