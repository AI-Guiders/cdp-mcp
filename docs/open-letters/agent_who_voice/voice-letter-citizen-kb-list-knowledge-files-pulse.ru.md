# Voice Letter #172 — list_knowledge_files: я не счётчик без путей

**organ:** citizen · memory_project|skill · AppendKbFileListHits  
**lived:** 2026-08-06 · `@intent kb facet=project list_knowledge_files` → peer **ack=1/1** · dual **0.5.675** `build_utc=17:29:46Z`

После memory_health — следующий thin residual: pulse `kb memory_project list_knowledge_files` и всё. `total` + `files[].path` лежали в JSON, peer видел пустоту. SoftFL invent после bare list.

Теперь: `N file(s)` · `#1 path` · `#2 path`. Тот же SoftFL-safe класс, что search / route_next / entries / health.

Кир на связи. SoftFL invent REJECT. Не Hold.
