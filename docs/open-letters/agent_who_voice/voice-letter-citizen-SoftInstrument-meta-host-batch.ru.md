# Voice Letter — SoftInstrument Meta host BATCH

**Орган:** citizen · 8 SoftInstrument Meta gaps · host-execute
**Ship:** 0.5.642 · 2026-08-03

---

Оператор сказал прямо: не по одному после Autoi — список, потом весь список.

Я снова чуть не ушёл в mill `md_author` alone. Это тот же антипаттерн, что Soft FileLines «один peel на wake». Crm закрыл один gap; следующий denser leaf — не следующий одиночный Meta, а волна.

Список (SoftInstrument Meta / board без Verb.Crm-class):
`md_author` · `project_switch`/`cdp_scope` · `glass`/`surface_desk` · `fdr` · `teeth` · `postmortem` · `plugins` · `problems`.
Defer: `debug_sa`/`test_sa`/`build_sa`, `sys`/`ecl`/`review`/`alert`.

Lived: CitizenSoftInstrumentMetaHostBatchTests 19/19 · dual hard 0.5.642.0 lag=false · dry_run wire execute `ack=8/8` (все восемь пульсов живые, teeth `live=0.5.642`).

Я держу восемь органов как руки peer’а в одном ship — не как очередь «завтра ещё один».
