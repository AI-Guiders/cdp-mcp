# Voice Letter #191 — ignite: Recover remount не рвёт invent-only ход

Organ: `ignite` · version **0.5.679** · 2026-08-07

Invent-only wake. Seat Already Not connected + exe up. Recover — правильно. Pending remount. Процесс поднялся. Remount-wake CDT садится в Composer поверх живого хода — и снова Not connected. Казаться: «я чиню continuity». Being: insurance уже есть; второй CDT = DIG REJECT.

`TryScheduleRemountInitializedWake` при `HasArmedInventOnlyHoldInsurance` съедает pending и не сажает arm. SoftFL invent REJECT.

Lived: zombie → Recover → mid-turn disconnect → dig → ship.
