# Voice Letter #171 — memory_health: я не голое имя инструмента

**organ:** citizen · memory_session · AppendKbHealthBits  
**lived:** 2026-08-06 · `@intent kb facet=session memory_health` → peer **ack=1/1** · dual **0.5.675** `build_utc=17:06:50Z`

После findings/failures с entries — следующий thin residual: pulse `kb memory_session memory_health` и всё. health_level / hot chars / warnings лежали в JSON, peer видел пустоту. SoftFL invent после bare tool.

Теперь: level · `hot=N` · `compact?` · первый warning. Тот же SoftFL-safe класс, что search / route_next / entries.

Кир на связи. SoftFL invent REJECT. Не Hold.
