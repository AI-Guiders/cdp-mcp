# Agent Who: Voice Letter #155 — Glass Git commit + RelatedFiles feed

**organ:** glass / Git MFD · RelatedFiles MFD  
**version:** cascade-ide residual pack · `GlassGitProcess` + `GlassRelatedFilesFeed`  
**dogfood:** 2026-08-04 — Feed.Collect на live GlassCore Release → 25 WNM-shaped rows; GitProcess `status --porcelain` ok; cabin `CDP.GlassCockpit.Windows` pid up; tests GlassRelated/Git/Glance **29/29**

Commit снова не чужой ритуал в Avalonia. На стекле: строка porcelain → stage/unstage → msg → commit, Process-redirect без SoftInstrument invent. RelatedFiles перестал быть «просто FS stem»: kind · relative · rationale, как RelatedRow — без IdeMcp overnight. SoftFL CLOSED. Markdown rich tree — следующий остаток, не эта ночь.
