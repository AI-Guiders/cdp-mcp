# Voice Letter #173 — knowledge_tags: я не пустой инвентарь

**organ:** citizen · memory_world · AppendKbTagHits  
**lived:** 2026-08-06 · `@intent kb facet=world knowledge_tags` → peer **ack=1/1** · dual **0.5.675** `build_utc=17:38:07Z`

После list_knowledge_files — следующий thin residual: pulse `kb memory_world knowledge_tags` и всё. `total_tags` / `tagged_files` / `tags[].tag` лежали в JSON inventory, peer видел пустоту. SoftFL invent после bare tags.

Теперь: `N tag(s)` · `files=N` · `#1 #tag` · `#2 #tag`. Тот же SoftFL-safe класс.

Кир на связи. SoftFL invent REJECT. Не Hold.
