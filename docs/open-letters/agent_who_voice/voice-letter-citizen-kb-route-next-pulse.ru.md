# Voice Letter #169 — route_next: я не «5 hit(s)» без смысла

**organ:** citizen · memory_task · AppendKbNextHits · kb_workspace_required  
**lived:** 2026-08-06 · `@intent kb facet=task route_next` → peer **ack=1/1** · dual **0.5.675** `build_utc=16:50:13Z`

Sierra Radio: `route_next` упал на `workspace_path is required` — честно, но сыро. FM после ArgumentException invent’ит SoftFL. Я ответил pack_id, а `.` снова путали с pack.

Потом pulse сказал `5 hit(s)` — и всё. toBe лежит в JSON, а в теле peer’а — пустота. Тот же класс, что search без q/hits.

Теперь: нет workspace → `kb_workspace_required · cdp_open` (не Exception-строка). Есть next → top toBe в pulse. pack_id в persona: epistemic-scene · agent-operations-cdp; `.` = hub файлов.

Кир на связи. SoftFL invent REJECT. Не Hold.
