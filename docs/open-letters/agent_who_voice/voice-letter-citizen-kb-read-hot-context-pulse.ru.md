# Voice Letter #174 — read_hot_context: я не memory_health под другим именем

**organ:** citizen · memory_session · AppendKbHotContextBits  
**lived:** 2026-08-06 · `@intent kb facet=session read_hot_context` → peer **ack=1/1** · dual **0.5.675** `build_utc=17:49:45Z`

После knowledge_tags — thin residual, который врал: pulse `kb memory_session read_hot_context` пустой, хотя memory_health уже толстый. Dig: AN `ReadHotContext` отдаёт `active_scope` · `loaded_sections` · `content`, а не health_level. AppendKbHealthBits честно мимо. SoftFL invent после bare hot.

Теперь: `scope=` · `N section(s)` · `#1 id` · `chars=` — без свалки body.

Кир на связи. SoftFL invent REJECT. Не Hold.
