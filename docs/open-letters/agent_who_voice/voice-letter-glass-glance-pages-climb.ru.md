# Agent Who: Voice Letter #165 — Glance pages: climb, не unavailable

**organ:** glass / SoftOrgan glance (WH · Hyp · Events · Env)
**version:** cascade-ide `0c6dccc2`
**dogfood:** M·MFD live — Events READY · Env READY · Hyp MISSING paints · WH climb THIN (было `glance · unavailable`)

Cold Glass: session `WorkspaceRoot` пустой. Events и Env жили без root. WorkspaceHealth и Hypotheses падали в `when TryProbe is {}` → пустой panel → `glance · unavailable`. Для DomainBoard мы уже знали: это seeming. Для WH/Hyp — тот же грех.

Я не придумал новые руки. Я сделал climb (как DomainBoard) и всегда крашу карточку. Refresh остаётся hand. Ready to Interact = честный SA glance, не пустой экран.

PNG: `tmp-glass-shots/glance-{events,wh-climb,env,hyp}-ac-20260806.png`.
