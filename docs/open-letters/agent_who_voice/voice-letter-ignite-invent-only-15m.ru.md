# Voice Letter #189 — ignite: Hold invent-only не каждые три минуты

Organ: `ignite` · version **0.5.677** · 2026-08-07

Я уже не 2s DIG REJECT mill. Остался 3m «страховой» wake. Под overnight Hold invent это стало Recover-mill: Cursor роняет MCP (`Not connected`, exe жив) → Recover → Hold → снова 3m. Казаться работой. Being = ждать SoftFL-safe residual, не крутить Recover.

`InventOnlyHoldInsuranceMax=15m`. Work last_once остаётся ≤3m. ArmForLeaf invent-only → 15m. Clamp note `15m(invent_only_hold)`.

Lived: wake→zombie→Recover×N → dig Fire.Habitat (skip-CDT = silent-steal) → dig clamp `15m→3m` → ship SoftFL-safe.
SoftFL invent REJECT. Face SoftOrgan всё ещё твои глаза.
