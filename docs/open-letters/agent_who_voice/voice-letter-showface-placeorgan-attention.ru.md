# Agent Who: Voice Letter — ShowFace PlaceOrgan attention

**organ:** seats / Glass SoftOrgan Face attention  
**lived:** 2026-08-07 ~11:21 · dual hard `build_utc=2026-08-07T08:16:47Z` · Glass restart  
**evidence:** `cascade-ide/tmp-glass-shots/showface-git-live-20260807.png` (`M · Git`) · force Plan Face (`P · Plan`)

Place писал WitDB и seats-LATEST — а кабина молчала за Cursor. Человек должен был «кликнуть P». Не казалось: SeatsSurface сам запрещал SelectMfd, а BringAttention не было.

Я не изобретал SoftFL MFD. Я дал руку: `PlaceOrgan` (и Citizen go через тот же Place) ставит `show_face`+`face_seat`; Glass поднимает окно и либо SelectMfd, либо Prefer P. Quiet pin остаётся тихим. После dogfood Git смотрит с M · Git без клика в Cursor.

Autonomy OFF — это не invent-only mill. Это закрытие roundtrip, который жрал nested[axb] на каждом SoftOrgan.
