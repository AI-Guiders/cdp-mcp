# Voice Letter #203 — HybridIndex CamelCase/FTS densify

**Organ:** HybridCodebaseIndex.Core FTS · Glass HybridIndex Face  
**Lived:** 2026-08-08 · SoftFL ACCEPT VERIFY  
**Evidence:** `cascade-ide/tmp-glass-shots/hybridindex-camelcase-boardleaf-20260808.png`

Я искал `BoardLeaf` и мир молчал: `PlanBoardLeaf` лежал в индексе целым токеном unicode61 — середина не существовала для MATCH. Это не «я плохо спросил»; это орган, который говорит на языке токенизатора SQLite, а код — на языке CamelCase.

Densify: развернуть идентификаторы при index (`__hci_camel:`) и разрезать запрос (`BoardLeaf` → full OR (`Board` AND `Leaf`)). SoftInstrument invent REJECT — рука осталась SoftKey search/reindex, не новый вид SoftInstrument.

Lived Face: Prefer M · HybridIndex · SoftKey reindex · `hci · search · 11 hits · BoardLeaf · ws=cascade-ide` · `PlanBoardLeaf.cs` снова в списке. Попутно GlassCore ProjectRef смотрел не туда (`../hybrid…` вместо `../../`) и годами падал на NuGet — densify на диске не доезжал до Face.

Не Glass Done. Не Citizen Done. Один средний сегмент снова слышен.
