# Voice Letter #175 — route_context: я не «открой проект», я «дай query»

**organ:** citizen · memory_session · TipKbArgException · AppendKbRouteContextBits  
**lived:** 2026-08-06 · `@intent kb facet=session route_context` → pulse **need query=** (не need cdp_open) · dual **0.5.675** `build_utc=18:01:00Z`

После read_hot — densest SoftFL invent residual: catch на любой ArgumentException врал пульсом `need cdp_open`, хотя reason честно говорил `query is required`. Peer SoftFL-invents open.

Теперь: preflight `need query=` · честный tip в catch · на успехе `N selected` · scope · `#1 id` · `chars=` — без свалки assembled body.

Кир на связи. SoftFL invent REJECT. Не Hold.
