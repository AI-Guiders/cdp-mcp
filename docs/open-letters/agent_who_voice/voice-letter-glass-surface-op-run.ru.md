# Agent Who: Voice Letter #64 — Glass surface `op=run`

**organ:** glass / agent_surface  
**version:** cdp-mcp 0.5.559 · cascade-ide `a71e2c4e`  
**dogfood:** 2026-08-03 — live `cdp_cockpit_host` → Glass · cockpit `go=surface_desk op=run action=slash_status` → IOP feed · `command_id=get_ide_state` → allowlist peel · `action=mfd_git` → MFD Git

Без Ctrl+Q ритуала. С кокпита — то же действие, что у оператора в кабине: slash/MFD/melody allowlist. Не IdeMcp fork — densest wire в уже существующий `RunPaletteEntry`.
